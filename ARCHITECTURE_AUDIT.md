1️⃣ Краткое архитектурное резюме
Архитектура минималистична, но слои и границы ответственности размазаны: CLI, парсинг, планирование и Inbox переплетены через глобальные объекты и прямые вызовы без явных контрактов. Бизнес-логика не отделена от I/O, зависимости инициализируются имплицитно в точках использования.
Главная системная проблема: отсутствие явных контрактов и уровня композиции/оркестровки — функции парсера и планировщика дергаются напрямую из CLI, что делает расширение и тестирование целостного сценария хрупким.

2️⃣ Таблица слабых мест
Модуль / слой | Проблема | Почему это проблема | К чему приведёт
CLI (main.py) | Глобальное состояние (Inbox, TZ), смешение I/O и бизнес-логики | Точка входа знает детали планирования/парсинга, сложно подменять или тестировать без ввода/вывода | Рост CLI/UX приведёт к дублированию логики и невозможности переиспользовать ядро
Parsing (parser.py) | Неявный контракт через raw/tz/now, встроенный re-инференс длительности | Отсутствие DTO/протокола для входных параметров; логика инференса дублирует duration_extractor | Расширение языков/правил потребует править несколько мест и усложнит тесты
Scheduling (scheduler.py) | Модуль сам делит fixed/flexible и работает на списках Event без явной фабрики/валидатора | Нет проверки входных данных (duration<=0, WorkDay валиден) и нет интерфейса для источников событий | Невозможно безопасно подключить новые источники событий; ошибки данных всплывут в рантайме
Config vs main | TZ/язык продублированы и не используются из config.py | Конфигурация раскидана по модулям, нет единого источника | При смене конфигурации нужно править код в нескольких местах
Inbox | Жёстко используется из CLI без интерфейса | Нельзя заменить хранилище или мокнуть в тестах | Любое подключение внешнего хранилища требует переписывать CLI/парсер

3️⃣ Критические архитектурные нарушения
📍 main.py: глобальные объекты Inbox/TZ и прямое создание WorkDay внутри CLI (строки 12–65). ❌ Нарушение инверсии зависимостей — точка входа создаёт и связывает доменные объекты и хранение. 💥 При добавлении других точек входа (API, боты) придётся копировать/рассинхронизировать логику композиции.
📍 parsing/parser.py: инференс длительности по time range реализован инлайн через re (строки 21–37), дублируя ответственность duration_extractor. ❌ Нарушение единственности ответственности и контракта модуля. 💥 Добавление новых форматов диапазонов потребует менять несколько мест и легко породит несовместимые правила.
📍 scheduling/scheduler.py: функция autoschedule сама классифицирует события и управляет очередью/overflow (строки 12–79) без явного интерфейса на вход/выход. ❌ Высокое зацепление и отсутствует уровень сервисов/адаптеров. 💥 При расширении логики (приоритеты, календари, ограничения) придётся ломать существующую функцию и тестировать целиком, а не модулями.

4️⃣ Рекомендации по исправлению
- Ввести слой композиции/сервисов (например, Application Service) между CLI и модулями: вынести создание Inbox, WorkDay, TZ, конфигурацию и последовательность вызовов в отдельный модуль, который можно переиспользовать в API/ботах.
- Сделать явные DTO/протоколы для входа/выхода парсера и планировщика: структуру входных параметров (raw text, now, tz) и результата парсинга; расписание — отдельная сущность с объяснением причин (можно использовать существующие dataclass в scheduling.explain).
- Убрать дублирование инференса длительности: расширить duration_extractor для диапазонов времени и вызывать его из parser, исключив встроенный re-блок.
- Централизовать конфигурацию: потреблять TZ/язык/DEFAULT_DURATION из autocalendar.config (или отдельного settings-модуля) и передавать через зависимости, а не создавать в main.
- Добавить валидацию входных Event/WorkDay (например, в фабрике событий или в normalize.py) и разделить шаги: normalize → validate → schedule. Это снизит хрупкость autoschedule при расширении.
- Ввести интерфейс/абстракцию InboxRepository, чтобы можно было подменять in-memory реализацию и подключать внешние хранилища без переписывания CLI/парсера.

5️⃣ Архитектурные риски на будущее
- Множественные точки входа будут копировать конфигурацию и пайплайн, т.к. нет слоя композиции.
- Расширение парсера новыми правилами приведёт к расхождению между extractors и inline-логикой в parser.py.
- При изменении модели событий (приоритеты, длительность, статусы) autoschedule придётся переписывать из-за отсутствия контрактов и проверок.
- Подключение реального Inbox/календарей потребует менять CLI и планировщик напрямую из-за отсутствия репозиториев/адаптеров.
